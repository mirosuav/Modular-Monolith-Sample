namespace RiverBooks.SharedKernel.Authentication;

public static class UserClaims
{
    public const string Email = nameof(Email);
    public const string Id = nameof(Id);
    public const string TokenId = "sid";
}
